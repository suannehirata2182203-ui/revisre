# 🔧 Устранение неполадок

## Проблема: "Application failed to respond"

### Возможные причины и решения:

#### 1. Проверьте переменные окружения на Railway

Убедитесь, что в настройках проекта Railway установлены следующие переменные:

```
TARGET_URL=https://beyondchargers.com
USE_PROXY=true
PROXY_HOST=185.162.130.86
PROXY_PORT=10000
PROXY_USER=UInVgOaurISMxHUOMkfD
PROXY_PASS=xnElmQSosaC9sekBD1SRzgqgBWcj2HsZ
```

**Как проверить:**
1. Откройте проект на Railway
2. Перейдите в Settings → Variables
3. Убедитесь, что все переменные установлены

#### 2. Проверьте логи приложения

**На Railway:**
1. Откройте проект
2. Перейдите в раздел "Deployments"
3. Выберите последний деплой
4. Откройте вкладку "Logs"

**Что искать в логах:**
- `✅ Proxy agent configured` - прокси настроен правильно
- `❌ Error configuring proxy` - проблема с настройкой прокси
- `[PROXY ERROR]` - ошибки при проксировании запросов
- `🚀 Educational Reverse Proxy Server running` - сервер запущен

#### 3. Проблемы с прокси-сервером

Если прокси-сервер недоступен, приложение может работать без него:

**Вариант 1: Отключить прокси**
```
USE_PROXY=false
```

**Вариант 2: Проверить доступность прокси**
- Убедитесь, что прокси-сервер доступен
- Проверьте правильность учетных данных
- Проверьте, что порт открыт

#### 4. Проблемы с таймаутами

Если запросы занимают слишком много времени:
- Таймаут установлен на 30 секунд
- Если нужно увеличить, измените в `server.js`:
  ```javascript
  timeout: 60000, // 60 секунд
  proxyTimeout: 60000,
  ```

#### 5. Проблемы с SSL сертификатами

Если возникают ошибки SSL:
- Проверьте, что `secure: true` в конфигурации прокси
- Убедитесь, что целевой сайт использует валидный SSL сертификат

## Проблема: "502 Bad Gateway"

### Решения:

1. **Проверьте доступность целевого сайта**
   - Убедитесь, что `https://beyondchargers.com` доступен
   - Попробуйте открыть его в браузере

2. **Проверьте настройки прокси**
   - Убедитесь, что прокси-сервер работает
   - Проверьте учетные данные

3. **Попробуйте без прокси**
   - Временно установите `USE_PROXY=false`
   - Если работает без прокси, проблема в прокси-сервере

## Проблема: Страница загружается, но контент не отображается

### Решения:

1. **Проверьте консоль браузера**
   - Откройте Developer Tools (F12)
   - Проверьте вкладку Console на наличие ошибок
   - Проверьте вкладку Network на неудачные запросы

2. **Проблемы с CORS**
   - Некоторые ресурсы могут блокироваться политикой CORS
   - Это нормально для образовательной демонстрации

3. **Проблемы с относительными путями**
   - Код автоматически заменяет абсолютные URL на относительные
   - Если проблема сохраняется, проверьте логи

## Проблема: Приложение не запускается

### Решения:

1. **Проверьте Dockerfile**
   - Убедитесь, что Dockerfile присутствует в репозитории
   - Проверьте, что все зависимости указаны в `package.json`

2. **Проверьте порт**
   - Railway автоматически устанавливает переменную `PORT`
   - Убедитесь, что приложение использует `process.env.PORT`

3. **Проверьте зависимости**
   - Убедитесь, что все зависимости установлены
   - Проверьте `package.json` на наличие всех необходимых пакетов

## Полезные команды для отладки

### Локальная проверка:

```bash
# Установить зависимости
npm install

# Запустить локально
npm start

# С переменными окружения
TARGET_URL=https://beyondchargers.com USE_PROXY=false npm start
```

### Проверка подключения к прокси:

```bash
# Используя curl (если установлен)
curl -x http://USER:PASS@185.162.130.86:10000 https://beyondchargers.com
```

## Контакты для поддержки

Если проблема не решена:
1. Проверьте логи на Railway
2. Убедитесь, что все переменные окружения установлены
3. Попробуйте пересобрать проект (Redeploy на Railway)

---

**Помните:** Это образовательный проект. Используйте ответственно и этично!

