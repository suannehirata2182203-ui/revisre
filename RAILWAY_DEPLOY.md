# 🚂 Развертывание на Railway.com

## Быстрый старт

### 1. Подготовка репозитория

Убедитесь, что все файлы закоммичены в Git:

```bash
git add .
git commit -m "Initial commit: Educational reverse proxy"
git push origin main
```

### 2. Создание проекта на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Войдите в аккаунт (можно через GitHub)
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий

### 3. Настройка переменных окружения

В настройках проекта Railway (Settings → Variables) добавьте:

```
TARGET_URL=https://beyondchargers.com
USE_PROXY=true
PROXY_HOST=185.162.130.86
PROXY_PORT=10000
PROXY_USER=UInVgOaurISMxHUOMkfD
PROXY_PASS=xnElmQSosaC9sekBD1SRzgqgBWcj2HsZ
```

**Примечание:** Railway автоматически установит переменную `PORT`, её не нужно добавлять вручную.

### 4. Развертывание

Railway автоматически:
- Обнаружит `Dockerfile`
- Соберет Docker-образ
- Развернет приложение
- Предоставит публичный URL

### 5. Доступ к приложению

После развертывания Railway предоставит URL вида:
```
https://your-project-name.up.railway.app
```

Откройте этот URL в браузере.

## Проверка работы

1. Откройте URL вашего проекта
2. Вы должны увидеть страницу с предупреждением об образовательных целях
3. Нажмите "Access Proxied Site" для доступа к проксированному контенту

## Логи

Для просмотра логов в Railway:
- Перейдите в раздел "Deployments"
- Выберите последний деплой
- Откройте вкладку "Logs"

## Обновление

При каждом push в репозиторий Railway автоматически пересоберет и развернет новую версию.

## Troubleshooting

### Проблема: "Connection refused"
- Проверьте, что все переменные окружения установлены
- Убедитесь, что прокси-сервер доступен

### Проблема: "502 Bad Gateway"
- Проверьте логи в Railway
- Убедитесь, что `TARGET_URL` корректен
- Проверьте настройки прокси

### Проблема: Прокси не работает
- Убедитесь, что `USE_PROXY=true`
- Проверьте учетные данные прокси
- Проверьте доступность прокси-сервера

## Безопасность

⚠️ **Важно:**
- Не коммитьте учетные данные прокси в Git
- Используйте переменные окружения Railway
- Регулярно обновляйте зависимости
- Мониторьте логи на подозрительную активность

